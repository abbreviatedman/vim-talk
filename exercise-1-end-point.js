function isOdd(num) {
  return num % 2 === 1;
}

function isEven(num) {
  return num % 2 === 0;
}

function greet(person, weekday) {
  return 'Hello, ' + person + "! Hope you're having a great " + weekday + '.'
}
